# ============================================================
# 依赖清单（2026-07-11 全量审计：仅保留代码实际 import 的包）
# ============================================================

# ── 数据层 ──
akshare>=1.14.0           # A股/港股/美股数据（主数据源 + fallback 链）
baostock>=0.9.0           # A股行情/估值/财务（A股主数据源，稳定免费）
pywencai>=0.13.1          # 同花顺问财 NLP 客户端（A股 spot 兜底）
pytdx>=1.72               # 通达信实时行情（A股备用）
yfinance>=0.2.38          # 美/港股备用 + 美股财报日历
pandas>=2.1.0             # 数据处理核心
numpy>=1.26.0             # 数值计算
PyYAML>=6.0.1             # YAML 配置解析
ruamel.yaml>=0.18.0       # 保留注释的 YAML 写回（config_io 原子写）
requests>=2.31.0          # HTTP 请求（数据源 + 告警通道）

# ── Web 界面 / REST API ──
fastapi>=0.110            # API 框架（src/api/main.py）
uvicorn>=0.29             # ASGI 服务器

# ── 分析与回测 ──
ta>=0.11.0                # 技术指标（MA/MACD/RSI/布林带）
scipy>=1.11.0             # 信号处理（背离极值检测等）
backtrader>=1.9.78.123    # 回测引擎
matplotlib>=3.8.0         # CLI 估值图输出（main.py）

# ── 机器学习（ML 因子自学习）──
lightgbm>=4.1.0           # GBDT 模型（原生 API，无需 sklearn）
joblib>=1.3.0             # 模型序列化
pyarrow>=14.0.0           # parquet 数据集存储

# ── 自动化调度 ──
APScheduler>=3.10.4       # 定时任务

# ── 测试与代码规范 ──
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
ruff>=0.1.0

# ── 可选功能（按需安装，缺失时自动降级）──
# anthropic>=0.40.0        # AI 周报（需 ANTHROPIC_API_KEY；未装则模板模式）
# markdown>=3.5            # 周报 HTML 导出（未装降级 <pre>）
# fpdf2>=2.7.0             # 周报 PDF 直出（未装可用 HTML 打印为 PDF）
# tushare                  # 荐股逆向的历史截面数据源（需 token）
# futu-api                 # 富途实盘适配（需本地 OpenD 网关）
# tigeropen                # 老虎证券实盘适配

# ── 2026-07-11 审计移除（代码零引用）──
# SQLAlchemy / pymysql（数据库层从未落地）、openpyxl、reportlab、
# python-pptx、Pillow、responses、scikit-learn（lightgbm 用原生 API）、
# plotly（随 Streamlit 界面下线）、streamlit
